import { Routes, RouterModule } from '@angular/router';
import { AccountRechargeComponent } from './account-recharge.component';

const routes: Routes = [
  // {
  //   path: '',
  //   component: AccountRechargeComponent
  //  },
];

export const AccountRechargeRoutes = RouterModule.forChild(routes);
